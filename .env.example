# Copy this file to .env.local for local development overrides.
# In Vercel, set VITE_API_URL in the dashboard under Settings → Environment Variables.

# Example (replace with your actual Render URL after deployment):
VITE_API_URL=https://your-astra-engine.onrender.com
